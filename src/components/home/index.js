import React from "react";

const Home = () => {
  return (
    <div
      className=""
      style={{
        height: "90vh",
        width: "100vw",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "40px",
        fontWeight: "800",
      }}
    >
      this is redux project
    </div>
  );
};

export default Home;
